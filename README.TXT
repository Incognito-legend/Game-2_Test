------------------------------------------------------------------------
This is the project README file. Here, you should describe your project.
Tell the reader (someone who does not know anything about this project)
all he/she needs to know. The comments should usually include at least:
------------------------------------------------------------------------

PROJECT TITLE:
PURPOSE OF PROJECT:
VERSION or DATE:
HOW TO START THIS PROJECT:
AUTHORS:
USER INSTRUCTIONS:


x.scroll(0, -10); 

[ means (pos_x + dx, pos_y += dy) ]




getBackground().drawImage(largeImage, pos_x, pos_y);

[ means (largeImage, -1358, -2045); ]
  so pos_x = -1358 and pos_y = -2045, and the largeImage = the picture being used to visualize public class background 

[ largeImage ]

GreenfootImage largeImage = new GreenfootImage("map-2.png"); 

[ means largeImage.scale(largeImage.getWidth()*6, largeImage.getHeight()*6); ]

Large Image in and of itself is a picture being implemented into public class background, public background  




[ variable += variable; means variable (+ or -) variable ]
 